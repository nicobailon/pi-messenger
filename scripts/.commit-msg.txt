task-1: add canonical session/monitor state model without UI integration

- src/monitor/canonical/types.ts: Zod schemas + interfaces for
  CanonicalLifecycleState (queued/starting/running/waiting/completed/failed/canceled),
  CanonicalHealthState (active/idle/waiting/degraded/stuck/offline),
  CanonicalMonitorState (healthy/attention_needed/blocked/recovering/completed),
  RuntimeSessionInput, RuntimeHealthInput, CanonicalSession, SessionSections, AttentionView

- src/monitor/canonical/normalizer.ts: pure mapping utilities —
  mapSessionLifecycle (SessionStatus → CanonicalLifecycleState),
  mapHealthState (HealthStatus → CanonicalHealthState),
  normalizeSession, normalizeSessions, deriveSections,
  deriveAttentionView, deriveMonitorState

- src/monitor/canonical/index.ts: barrel export

- tests/monitor/canonical/normalizer.test.ts: 27 focused tests covering
  exact lifecycle mapping, exact health mapping, section derivation,
  attention derivation, Zod schema validation

- docs/canonical-state-model.md: developer reference

No existing types modified; backward compatible.
